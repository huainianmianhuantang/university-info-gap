# 大学信息差网站 · 优化报告

> 生成时间：2026-08-22 09:00（最终版）
> 本文档随工作进度持续更新，最终交付时包含全部完成项与验证状态。

## 一、视频资料扩充（39 校 × 116 条）

- [x] 每校新增 2 条生活/学习类视频：1 条「宿舍/校园生活」+ 1 条「学习/专业体验」，保留原宣传片。
- [x] 视频 schema 增加 `tags` 字段（宣传片 / 宿舍生活 / 学习日常），资源库视频 tab 支持标签徽标与筛选。
- [x] 三列网格布局（宽屏 3 列，≤1100px 2 列，≤640px 1 列）。
- [x] 视频来源如实标注（官方账号 / 转载 UP 主）。
- [ ] B 站 BV 全量 API 复核（后台慢速校验进行中，速率受限，完成后同步替换失效条目）。

## 二、选校测评 + AI 建议

- [x] `/quiz/` 12 题兴趣问卷（兴趣、擅长、学习方式、职业、实践接受度、城市、生活、类型、成本、转专业、距离、性格）。
- [x] 规则推荐引擎 `src/lib/recommender.ts`：兴趣画像 → 专业方向 → 5-8 所匹配高校（含匹配专业）。
- [x] 结果保存 localStorage，支持重新测评、一键复制结果。
- [x] 三入口：首页 hero CTA、导航、资源库视频 tab 引导卡。
- [x] DeepSeek 大模型接入：`/api/advice` 服务端接口（`@astrojs/node` 适配器），Key 存 `.env` 不入库，接口异常自动回退规则引擎。

## 三、站点优化

- [x] 大学库：卡片视频/资料数量徽标、勾选对比（2-3 所）→ `/compare/` 并排对比页、排序（名称/省份/视频数/资料数）、空结果提示。
- [x] 大学库：学校收藏（心愿单）——卡片星标、只看收藏筛选、详情页收藏按钮（localStorage）。
- [x] 详情页：相关学校推荐、栏目锚点滚动高亮、待补充栏目提示、JSON-LD 结构化数据。
- [x] 首页：测评 CTA、真实统计数字、重点大学固定展示。
- [x] 移动端：汉堡导航、响应式网格。
- [x] SEO：robots.txt、canonical、OG/Twitter meta、sitemap（含 /quiz/、/compare/）、PWA manifest。
- [x] 404 页面、回到顶部按钮、页脚链接完善。
- [x] 内容：39 校真实概况正文与列表摘要；新增 3 篇专题文章（宿舍视频甄别、测评使用说明、转专业横向速览）。
- [x] 资源库视频 tab：分组可折叠卡片 + 总数统计提示。
- [x] README 重写（功能、维护、部署、AI 升级说明）。
- [x] 安全：AI Key 改为运行时读取（`process.env` / `.env` 动态加载），构建产物不含 Key。
- [x] 生产构建 QA：`node dist/server/entry.mjs` 可独立运行（本沙箱对外 HTTPS 受限，部署到正常 Node 环境不受影响）。

## 四、验证状态

- [x] `npm run build` 通过（51 页）。
- [x] 关键页面 HTTP 200 冒烟（首页/大学库/资源库/测评/对比/详情/文章/投稿/关于）。
- [x] 全站内链检查通过（56 个 URL，无真实死链）。
- [ ] B 站 78 条新增视频 API 复核（进行中）。

## 五、待办 / 建议

- B 站视频复核完成后同步标题与来源；失效视频替换或标记。
- 部署到公网前将 `SITE.url` 改为正式域名（已支持 `PUBLIC_SITE_URL` 环境变量）。
- 后续接入更多学校资料（当前仅东北大学有文件资料），欢迎投稿。
- 若需升级建议质量，可在 `.env` 调整 `AI_MODEL`。
- 公网分享可运行 `scripts/start-tunnel.ps1`（Cloudflare 临时隧道，生成 trycloudflare.com 链接）。
